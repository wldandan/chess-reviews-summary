#!/bin/bash
# post-commit hook: auto-push after each commit

GIT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/.."
cd "$GIT_DIR"

# Only push if there are commits
if git rev-parse HEAD >/dev/null 2>&1; then
    git push origin main 2>&1
fi
